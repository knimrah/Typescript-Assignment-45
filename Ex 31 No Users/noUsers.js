"use strict";
//Exercise 31 No users
let noUsers = ['Admin', 'William', 'Alexander', 'tervor', 'Steven'];
if (noUsers.length === 0) {
    console.log('We need to find some users.');
}
else
    noUsers = [];
console.log('we need to find some users.');
console.log(' All users have been removed ' + noUsers.length);
